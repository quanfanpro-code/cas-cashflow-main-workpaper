# 需求文档：cas-cashflow-main-workpaper 技能全面复核（代码 + 业务）

日期：2026-08-16
流程：requirement-workflow 六阶段 · 阶段 1 需求澄清

## 一、背景与问题

用户要求对 `cas-cashflow-main-workpaper`（现金流量表主表工作底稿法技能）做与 cas-cashflow-indirect 相同标准的全面复核：代码 + 业务口径一起查，发现低级逻辑错误就顺手修复，文档同步，最后产出复核报告与验证证据。执行约定经用户确认全部照搬上一项目：

1. 复核 + 修复 + 同步相关文档；
2. 低级错误和已确认缺口都修；业务规则口径属于复核对象，有依据即可改；
3. 全程串行，不派子代理；
4. 每个修复先写失败测试再最小实现，再定向回归；
5. 全量验证含性能测试、自检、编译、编码检查；
6. 修复顺序按风险从高到低；
7. 交付时保留变更，不做 git 操作；
8. 修改前全量备份（已完成）；不删除文件、不新增依赖、不新增计划外功能。

## 二、技能现状（已核实事实）

1. 技能位于 `D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-main-workpaper`；`C:\Users\27651\.dsh\skills` 是指向该工作区的目录联接，改一处即全局生效。
2. 全量备份：`C:\Users\27651\BackUp\cas-cashflow-main-workpaper_20260816_20260816_1201`（380 个文件，与源一致）。
3. 基线：217 项普通测试全部通过（6.85 秒）；性能测试 1 项通过（1.64 秒）；启动器帮助正常；编译通过。
4. 技能已有 2026-08-02 至 08-03 三轮复核历史（README 更新记录），本轮在其基础上做独立复核，不把历史结论当正确性前提。
5. 结构：六套规则包（general、bank、securities、insurance 2018、insurance 2023、other_financial）＋六套公式核验 JSONL＋schema；代码模块约 20 个，含 input_adapter、ledger_reconciliation、statement_mapping、adjustment_bridge、semantic_facts、fact_extraction、item_calculators、completeness_check、pipeline、output。
6. 本轮已初步确认一个正表级逻辑错误并完成最小复现：**期初/期末现金及现金等价物余额行会把长期定期存款、期限不明的定期存款重新纳入**。`item_calculators.py::_select` 的 `cash_equivalent_balance` 在事实没有 `cash_equivalent` 标签时，仅凭科目名称包含现金科目就兜底纳入，绕过了 `extract_facts` 对"超过三个月剔除、期限不明先剔除"的标记（最小样例：仅有"银行存款-一年期定期存款"100元时，正表期初、期末现金余额行均列 100 元，本应列 0）。
7. 另发现待核实的低风险点：`validate_formula_registry.py --all-rule-packs` 仍强制要求 `--registry`，且只加载所给登记文件就对比全部规则包，会输出大量"missing"假阴性；运行时按行业加载各自登记文件的路径不受影响，需确认脚本是否只是开发工具用法问题。
8. 输入金额处理无精度上限校验：超过两位小数会四舍五入而不报错；全角负号和全角逗号不支持（会报错，不静默）；路径选择器把重要性金额小数直接截断为分。均待逐项判断是否修复。

## 三、功能范围（本次复核与修复要覆盖的）

1. 代码层：输入读取与公式缓存、科目映射与账表桥、一借一贷与余额表核对、行业与保险格式识别、语义事实、资本性应付款/资本化职工薪酬、进项税互斥、规则加载与门禁、逐项目计算与占用、完整性矩阵、AI 合同与双门槛、运行状态机、输出与留痕。
2. 业务层：六套规则包逐行业核对正表项目、组成公式、方向、总额/净额口径、非现金剔除、特殊事项与受限资金处理；重点复核一般企业经营/投资/筹资公式和金融企业专用项目是否与适用报表格式一致。
3. 文档一致性：SKILL.md、README、四份协议、需求覆盖表与代码行为一致，清理"文档承诺但入口不可达"。
4. 输出层：两张可见工作表、无隐藏无外链、正表冻结数值、底稿反算公式、双向链接、上期搬取与未匹配提示、暂编/最终状态。
5. 上轮已修能力回归验证：资本性应付款形成/支付中和、进项税互斥、受限资金、保险格式、金融规则包等必须有测试证据，不凭 README 声明。

## 四、明确不做

1. 不新增计划外功能；不删任何文件（含缓存）；不做 git 写操作；不修改其他技能目录。
2. 不重新设计五套金融规则包的项目结构（除非发现与正式格式冲突的明确证据）。
3. 不把需要客户资料才能判断的真实业务场景当作可自动修复项。
4. 不使用子代理；不新增运行依赖。

## 五、约束与风险

1. 先备份再修改；备份已存在，本轮新文档会补入备份目录。
2. 代码修改阶段遵守 ponytail 最小改动原则（已在上个项目加载，本轮继续生效）。
3. 全部验证在 Windows、Python 3.14 环境；测试从技能根目录运行。
4. 交流、文档、注释中文。
5. 业务结论依据优先级：技能内规则与公式核验 → 财政部格式文件与知识库 → 现行有效原文；冲突时以财政部现行有效原文为准。拿不准的重大口径暂停向用户确认。
6. 风险：六套规则包体量大，逐行业公式复核工作量大；金融企业专项项目专业性强，本轮以"结构、方向、互斥、可达性、与正式格式一致"为检查目标，不替代实务判断。
7. 风险：无真实客户样本，用合成数据验证；真实表样兼容留作残余风险。

## 六、验收标准

1. 本轮发现的问题全部闭环，低级逻辑错误零遗留（已确认的正表现金余额 bug 必须修复并有失败测试）。
2. 六套规则包逐套通过结构与公式抽查：项目方向、小计、净额、互斥、引用存在性、登记门禁无静默缺口。
3. 文档承诺与代码行为一致；README 变更日志更新。
4. 测试证据：
   - 普通测试全量通过；
   - 性能测试实跑通过；
   - 启动器、编译、编码检查通过；
   - 每个修复先 RED 后 GREEN。
5. 交付物：`docs/reviews/2026-08-16-全面复核.md` 复核报告 + 证据表/问题台账 + 更新后的 README/SKILL/协议。
6. 修改文件清单与备份比对一致，删除数 0。

## 七、调研摘要与来源

| 来源 | 结论 |
|---|---|
| `README.md` 三轮更新记录 | 历史修复与"不修改"决定清单 |
| 四份协议、`references/验收/需求覆盖.md` | 当前承诺清单 |
| 本轮代码通读 | 输入→核对→映射→桥接→事实→计算→完整性→输出全链路已读 |
| 本轮实测 | 217 项测试绿；性能 1 项绿；定期存款正表余额 bug 最小复现成立 |
| 用户确认 | 执行约定与上一项目完全一致 |

## 八、待确认项

用户已确认执行约定照搬，无其余待确认事项；CHECKPOINT 1 等待批准。
