# cas-cashflow-main-workpaper 全面复核与修复实施计划

> **For agentic workers:** Read `subagent-driven-development.md` when the user authorizes subagents; otherwise read `executing-plans.md` and implement task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按"业务规则先行、分层串行"完成 cas-cashflow-main-workpaper 的代码与六套规则包全面复核，修复已确认问题并全量验证。

**Architecture:** 先做四层复核并写问题台账，再按风险从高到低 TDD 修复 F1、T1、T2、T3，再处理复核新发现，最后文档同步与全量回归。

**Tech Stack:** Python 3.14（Windows）、openpyxl、jsonschema、pytest。

## Global Constraints

- 所有命令在技能根目录 `D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-main-workpaper` 下执行。
- 修改现有文件只用 edit；修改前备份已存在 `C:\Users\27651\BackUp\cas-cashflow-main-workpaper_20260816_20260816_1201`。
- 不执行 git 写操作；不删除任何文件；不用子代理；不新增依赖；不加计划外功能。
- 中文交流与注释；配置/JSON 用 UTF-8 BOM；SKILL.md 无 BOM。
- 每个修复：RED（先写失败测试并确认失败原因正确）→ GREEN（最小实现）→ VERIFY（定向回归）。
- 建议提交点仅记录，不执行。

---

### Task 1: 第一层六套规则包业务复核

**Files:**
- Create: `docs/reviews/2026-08-16-六套规则包证据表.md`
- Create: `docs/reviews/2026-08-16-问题台账.md`
- Read: `rules/*.json`（6套）、`references/公式核验/*.jsonl`（6套）、`rules/schema.json`
- Read: `src/cashflow_main/semantic_facts.py`、`fact_extraction.py`、`item_calculators.py`、`build_financial_rule_packs.py`（按需）

- [ ] **Step 1：逐套登记项目与组成统计**
  对 general、bank、securities、insurance、insurance_2023、other_financial 六套，分别统计项目数、组成数、net_fact_amount 数、subtotal 数，写入证据表。
- [ ] **Step 2：逐套自动交叉检查**
  用脚本检查：每个 `tags_any`/`positive_tags_any`/`negative_tags_any` 的标签是否能在 `semantic_facts.py` 的 `add(...)` 或 `fact_extraction.py` 的 tag 中出现（悬空分类）；每个 `account_groups` 引用是否存在于 `account_groups`；subtotal 引用是否已定义且 display_order 小于本项目；净额正负标签是否齐全；exclusive 组成的 selectors 是否有交叠风险（同名事实标签）。
- [ ] **Step 3：一般企业公式手算抽查**
  对 CFO-04（营业成本＋进项税＋存货增加＋应付减少＋预付增加－非现金修正±资本性中和）、CFO-05、CFO-06、CFI-06、CFF、FX、OPENING/CLOSING-CASH 手算方向并记录。
- [ ] **Step 4：金融企业专项抽查**
  抽查银行"客户贷款及垫款净增加额"、证券"为交易目的持有的金融资产"与"融出资金"方向、保险新旧格式"收到原保险合同保费"与"支付赔付款"、其他金融企业"融资租赁"项目是否与核验记录方向一致；检查取得/处置营业单位净额并入其他投资活动的实现。
- [ ] **Step 5：发现写入问题台账**
  每条问题带 `文件:行号` 证据与建议修复方向。

**Verify:**
```
python -X utf8 scripts/validate_formula_registry.py --registry references/公式核验/general_enterprise_v1.jsonl --rule-pack rules/general_enterprise_v1.json --require-zero-unresolved
```
（依次替换 bank、securities、insurance、insurance_2023、other_financial 六组重跑）
预期：六组均 records 非零、missing_evidence=0、orphan_rule=0、退出码 0。

---

### Task 2: 第二层代码正确性复核

**Files:**
- Modify: `docs/reviews/2026-08-16-问题台账.md`（追加）
- Read: `src/cashflow_main/*.py` 全部

- [ ] **Step 1：金额与输入**
  核对 `money.py` 的单位/精度、`select_paths.py` 金额换算、`input_adapter.py` 的表头识别、公式缓存、隐藏表引用、空白金额。
- [ ] **Step 2：核对与映射**
  核对 `ledger_reconciliation.py` 的层级归并与余额方程式；`statement_mapping.py` 的唯一映射、控制行排除、账面报表方向。
- [ ] **Step 3：事实与资本性分析**
  核对 `semantic_facts.py` 标签顺序、冲突与现金事实；`fact_extraction.py` 定期存款/受限资金标记、`_capital_liability_analysis` 形成/支付/中和、residual 与 ambiguous。
- [ ] **Step 4：计算与完整性**
  核对 `item_calculators.py` 的选择/方向/净额/占用/subtotal；`completeness_check.py` 未分配/重复/小计/现金变动差异；`rule_loader.py` 门禁。
- [ ] **Step 5：状态机与续跑**
  核对 `pipeline.py` 状态转移、输入哈希、暂编/最终、确认流程；`storage.py` 原子写；`__main__.py` 与 `engine_launcher.py` 命令路径。
- [ ] **Step 6：发现写入台账**

**Verify:**
```
python -X utf8 -m pytest tests -q --no-header
```
预期：全部通过（本任务只读不改）。

---

### Task 3: 第三层文档一致性复核

**Files:**
- Modify: `docs/reviews/2026-08-16-问题台账.md`（追加）
- Read: `SKILL.md`、`README.md`、`CONTEXT.md`、`references/协议/*.md`、`references/验收/需求覆盖.md`

- [ ] **Step 1：SKILL.md 承诺逐条对代码**
  固定输入、执行流程、自动判断双门槛、异常处理、输出两张表、红线，逐条定位实现。
- [ ] **Step 2：README 三轮历史承诺对代码**
  重点：定期存款"先剔除"、其他货币资金确认、资本性应付款形成/支付、进项税互斥、上期只搬取、无隐藏无外链。
- [ ] **Step 3：协议与需求覆盖表对代码**
  逐条核对四份协议和需求覆盖表；发现"文档承诺但入口不可达"写入台账。
- [ ] **Step 4：CONTEXT 术语一致性**
  术语与代码行为是否一致。

**Verify:**
```
python -X utf8 -m pytest tests/test_cli_and_skill.py tests/test_package_contract.py tests/test_self_contained.py -q --no-header
```
预期：全部通过。

---

### Task 4: 第四层输出与留痕复核

**Files:**
- Modify: `docs/reviews/2026-08-16-问题台账.md`（追加）
- Read: `src/cashflow_main/output.py`、`audit_trace.py`

- [ ] **Step 1：正表**
  核对两张表顺序、冻结数值、行次留空、上期搬取与未匹配提示、双向内部链接、无公式正表。
- [ ] **Step 2：底稿**
  核对逐项目组成、来源、方向、三类调整、未命中项、复核公式反算、状态与完整性结论。
- [ ] **Step 3：留痕**
  核对 `calculation.json`、`audit_trace.json`、`state.json`、`ledger_reconciliation.json` 的写入与续跑复用。
- [ ] **Step 4：发现写入台账**

**Verify:**
```
python -X utf8 -m pytest tests/test_output.py tests/test_visible_verification_workpaper.py tests/test_storage_and_pipeline.py -q --no-header
```
预期：全部通过。

---

### Task 5: F1 修复正表现金余额行的定期存款兜底纳入

**Files:**
- Modify: `src/cashflow_main/item_calculators.py`（`_select` 的 cash_equivalent_balance 分支）
- Test: `tests/test_restricted_cash.py`（新增测试）

**Interfaces:**
- Consumes: `cash_equivalent_balance` 现有选择器接口不变。
- Produces: OPENING-CASH/CLOSING-CASH 只统计 `cash_equivalent` 标签事实。

- [ ] **Step 1：写失败测试**
  在 `tests/test_restricted_cash.py` 文件顶部补 `import json`，并新增：

```python
def _general_rule_pack():
    registry = {}
    registry_path = Path("references/公式核验/general_enterprise_v1.jsonl")
    for line in registry_path.read_text(encoding="utf-8-sig").splitlines():
        if line.strip():
            record = json.loads(line)
            registry[str(record["verification_id"])] = record
    from cashflow_main.rule_loader import load_rule_pack
    return load_rule_pack(Path("rules/general_enterprise_v1.json"), registry)


def test_cash_balance_items_ignore_long_term_and_undetailed_deposits():
    from cashflow_main.item_calculators import calculate_items

    bundle = NormalizedInputBundle(
        audited_balance_sheet=(),
        audited_income_statement=(),
        trial_balance=(
            AccountBalance("100201", "银行存款", 10_000, 0, 0, 10_000),
            AccountBalance("100298", "银行存款-一年期定期存款", 5_000, 0, 0, 5_000),
            AccountBalance("100299", "银行存款-定期存款", 3_000, 0, 0, 3_000),
            AccountBalance("100202", "银行存款-冻结资金", 2_000, 0, 0, 2_000),
        ),
        journal_pairs=(),
        prior_cashflow=(),
    )
    facts = extract_facts(
        bundle,
        AdjustmentBridgeResult((), (), True),
        EnterpriseType.GENERAL,
        CASH_GROUPS,
    )
    calculation = calculate_items(_general_rule_pack(), facts)

    assert calculation.by_id["OPENING-CASH"].amount_minor == 10_000
    assert calculation.by_id["CLOSING-CASH"].amount_minor == 10_000
```

- [ ] **Step 2：运行确认失败**
```
python -X utf8 -m pytest tests/test_restricted_cash.py::test_cash_balance_items_ignore_long_term_and_undetailed_deposits -q --no-header --tb=short
```
预期：FAIL，实际金额为 18_000（100+50+30 元），证明长期与期限不明定期存款被错误纳入。

- [ ] **Step 3：最小实现**
  把 `item_calculators.py::_select` 中 `cash_equivalent_balance` 分支改为只认标签：

```python
    elif operation == "cash_equivalent_balance":
        period_kind = {"opening": "opening", "closing": "closing"}.get(str(selector.get("period")))
        return tuple(
            f
            for f in facts.values()
            if f.metadata.get("kind") == period_kind
            and "cash_equivalent" in f.tags
            and not (
                selector.get("exclude_restricted", True)
                and "restricted_cash" in f.tags
            )
            and _matches(f, selector, account_groups)
        )
```

- [ ] **Step 4：运行确认通过**
```
python -X utf8 -m pytest tests/test_restricted_cash.py::test_cash_balance_items_ignore_long_term_and_undetailed_deposits -q --no-header
```
预期：PASS。

- [ ] **Step 5：定向回归**
```
python -X utf8 -m pytest tests/test_restricted_cash.py tests/test_general_complete_case.py tests/test_end_to_end_general.py tests/test_financial_complete_cases.py -q --no-header
```
预期：全部 PASS；金融企业六套规则包不因去掉名称兜底而改变既有结果（其现金科目组已产生 cash_equivalent 标签）。

- [ ] **建议提交点（不执行）**：`fix: cash balance items honor non-cash-equivalent deposit tags`

---

### Task 6: T1 金额精度显式校验与全角符号

**Files:**
- Modify: `src/cashflow_main/money.py`
- Test: `tests/test_money_and_contracts.py`（新增测试）

- [ ] **Step 1：写失败测试**

```python
def test_to_minor_units_rejects_fraction_beyond_unit_precision():
    with pytest.raises(ValueError, match="小数"):
        to_minor_units("1.234", "元")

def test_to_minor_units_accepts_allowed_precision_by_unit():
    assert to_minor_units("1.23456", "万元") == 1_234_560

def test_to_minor_units_accepts_full_width_minus_and_comma():
    assert to_minor_units("－1，234.56", "元") == -123_456
```

- [ ] **Step 2：运行确认失败**
```
python -X utf8 -m pytest tests/test_money_and_contracts.py -q --no-header --tb=short
```
预期：FAIL（当前 1.234 静默四舍五入为 123，不报错；全角符号报错）。

- [ ] **Step 3：最小实现**
  `money.py` 中：

```python
_UNIT_DECIMAL_PLACES = {
    "元": 2,
    "千元": 5,
    "万元": 6,
    "百万元": 8,
}

def to_minor_units(value: object, display_unit: str) -> int:
    if display_unit not in UNIT_FACTORS:
        raise ValueError(f"不支持的金额单位：{display_unit}")
    text = str(value).replace(",", "").replace("，", "").replace("－", "-").strip()
    try:
        amount = Decimal(text)
    except InvalidOperation as exc:
        raise ValueError(f"无法识别金额：{value}") from exc
    exponent = amount.as_tuple().exponent
    if isinstance(exponent, int) and exponent < 0 and -exponent > _UNIT_DECIMAL_PLACES[display_unit]:
        raise ValueError(f"金额小数位超过{display_unit}允许精度：{value}")
    minor = amount * UNIT_FACTORS[display_unit] * Decimal("100")
    return int(minor.quantize(Decimal("1"), rounding=ROUND_HALF_UP))
```

- [ ] **Step 4：运行确认通过**
```
python -X utf8 -m pytest tests/test_money_and_contracts.py -q --no-header
```
预期：PASS。

- [ ] **Step 5：定向回归**
```
python -X utf8 -m pytest tests/test_input_adapter.py tests/test_end_to_end_general.py tests/test_financial_complete_cases.py -q --no-header
```
预期：全部 PASS。

- [ ] **建议提交点（不执行）**：`fix: reject silently-rounding amounts and support full-width number signs`

---

### Task 7: T2 路径选择器重要性金额四舍五入

**Files:**
- Modify: `scripts/select_paths.py`
- Test: `tests/test_cli_and_skill.py`（新增测试）

- [ ] **Step 1：写失败测试**

```python
def test_materiality_minor_rounds_half_up_instead_of_truncating(tmp_path):
    import importlib.util
    spec = importlib.util.spec_from_file_location(
        "select_paths", Path("scripts/select_paths.py").resolve()
    )
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)

    assert module._materiality_minor("1.999") == 200
    assert module._materiality_minor("1.994") == 199
    assert module._materiality_minor("1，000.5") == 100_050
    with pytest.raises(ValueError, match="正数"):
        module._materiality_minor("0")
```

- [ ] **Step 2：运行确认失败**
```
python -X utf8 -m pytest tests/test_cli_and_skill.py::test_materiality_minor_rounds_half_up_instead_of_truncating -q --no-header --tb=short
```
预期：FAIL（`_materiality_minor` 不存在）。

- [ ] **Step 3：最小实现**
  在 `scripts/select_paths.py` 顶部 import 补 `ROUND_HALF_UP`，新增纯函数并把 GUI 流程改用它：

```python
def _materiality_minor(raw: str) -> int:
    amount = Decimal(raw.replace(",", "").replace("，", "").strip())
    if amount <= 0:
        raise ValueError("实际执行重要性水平必须是正数")
    return int((amount * 100).quantize(Decimal("1"), rounding=ROUND_HALF_UP))
```
  把 `select_manifest` 中的 `int(materiality * 100)` 改为 `_materiality_minor(raw)`。

- [ ] **Step 4：运行确认通过**
```
python -X utf8 -m pytest tests/test_cli_and_skill.py -q --no-header
```
预期：PASS。

- [ ] **建议提交点（不执行）**：`fix: round materiality input to cents instead of truncating`

---

### Task 8: T3 公式登记校验脚本全量模式

**Files:**
- Modify: `scripts/validate_formula_registry.py`
- Test: `tests/test_formula_registry.py`（新增测试）

- [ ] **Step 1：写失败测试**

```python
def test_validate_script_all_rule_packs_checks_each_own_registry():
    import subprocess
    import sys

    script = Path("scripts/validate_formula_registry.py").resolve()
    result = subprocess.run(
        [sys.executable, "-X", "utf8", str(script), "--all-rule-packs", "--require-zero-unresolved"],
        capture_output=True,
        text=True,
        encoding="utf-8",
    )

    assert result.returncode == 0, result.stdout + result.stderr
    assert "missing_evidence=0" in result.stdout
    assert "orphan_rule=0" in result.stdout
```

- [ ] **Step 2：运行确认失败**
```
python -X utf8 -m pytest tests/test_formula_registry.py::test_validate_script_all_rule_packs_checks_each_own_registry -q --no-header --tb=short
```
预期：FAIL（当前 `--registry` 必填导致 usage error，或只加载一个登记文件导致大量 missing）。

- [ ] **Step 3：最小实现**
  改 `validate_formula_registry.py`：

```python
    parser.add_argument("--registry", type=Path)
    ...
    if args.all_rule_packs:
        records = load_registry(Path(__file__).parents[1] / "references" / "公式核验")
    else:
        if args.registry is None:
            parser.error("非全量模式必须提供 --registry")
        records = load_registry(args.registry)
```

- [ ] **Step 4：运行确认通过**
```
python -X utf8 -m pytest tests/test_formula_registry.py -q --no-header
```
预期：PASS。

- [ ] **Step 5：定向回归**
```
python -X utf8 -m pytest tests/test_financial_rule_packs.py tests/test_general_rule_pack.py -q --no-header
```
预期：全部 PASS。

- [ ] **建议提交点（不执行）**：`fix: formula registry validator all-rule-packs mode uses all registries`

---

### Task 9: 处理 Task 1–4 新发现的问题

**Files:**
- Modify: `docs/reviews/2026-08-16-问题台账.md`
- 其他文件按问题根因确定

- [ ] **Step 1：按严重度排序台账（阻断→误导→一致性→边界）**
- [ ] **Step 2：逐条 RED→GREEN→VERIFY**
  每个问题写失败测试并确认失败原因，再最小实现，再定向验证；重大口径拿不准时停止并向用户确认。
- [ ] **Step 3：更新台账状态与证据**

**Verify:**
```
python -X utf8 -m pytest tests -q --no-header
```
预期：全部通过；台账无悬挂项。

---

### Task 10: 文档同步与变更日志

**Files:**
- Modify: `README.md`（新增 2026-08-16 变更日志）
- Modify: `SKILL.md`、`references/协议/*.md`、`references/验收/需求覆盖.md`（按 Task 3/9 发现同步）
- Modify: `docs/requirements`、`docs/superpowers/specs`（如有偏差记录实际决策）

- [ ] **Step 1：汇总实际变更**
  从台账生成变更清单（文件、位置、行为变化）。
- [ ] **Step 2：写 README 变更日志**
  新增 `### 2026-08-16 · 全面复核（代码+业务）`，逐条写 F1/T1/T2/T3 与其余发现修复及验证数量。
- [ ] **Step 3：同步 SKILL 与协议**
  若代码行为变化影响协议或需求覆盖表，逐条更新并保持无 BOM 规则。
- [ ] **Step 4：检查编码**

**Verify:**
```
python -X utf8 -m pytest tests/test_cli_and_skill.py tests/test_package_contract.py tests/test_self_contained.py -q --no-header
```
预期：全部通过。

---

### Task 11: 全量回归验证（阶段 4 收口）

**Files:** 无（只运行验证）

- [ ] **Step 1：普通测试全量**
```
python -X utf8 -m pytest tests -q --no-header
```
预期：全部通过（≥217＋新增）。
- [ ] **Step 2：性能测试**
```
python -X utf8 -m pytest tests/test_performance.py -q --no-header
```
预期：1 项通过。
- [ ] **Step 3：启动器与编译**
```
python -X utf8 scripts/engine_launcher.py --help
python -X utf8 -m compileall -q src scripts
```
预期：帮助正常显示；编译退出码 0。
- [ ] **Step 4：公式登记校验（六组单包＋全量）**
```
python -X utf8 scripts/validate_formula_registry.py --all-rule-packs --require-zero-unresolved
```
预期：missing_evidence=0、orphan_rule=0、退出码 0。
- [ ] **Step 5：记录命令输出到复核报告草稿**

---

### Task 12: 阶段 5 验收审查与复核报告

**Files:**
- Create: `docs/reviews/2026-08-16-全面复核.md`
- Modify: `docs/reviews/2026-08-16-六套规则包证据表.md`、`docs/reviews/2026-08-16-问题台账.md`

- [ ] **Step 1：对照需求、设计、计划逐项验收**
- [ ] **Step 2：审查正确性、安全、数据损失风险、兼容、性能、可维护性、过度工程**
- [ ] **Step 3：审查测试是否验证真实行为，正常/边界/错误路径齐全**
- [ ] **Step 4：遗留问题按严重度修复（TDD）或写明"不改原因"**
- [ ] **Step 5：重新运行 Task 11 全部验证（不复用旧输出）并写入报告**

---

### Task 13: 阶段 6 交付

- [ ] **Step 1：重新运行最终验证并贴出证据**
- [ ] **Step 2：列出实际变更文件、验证结果、未解决风险、后续事项**
- [ ] **Step 3：按用户已确认的约定执行"保留变更、不做 git 操作"，并给出交付摘要**

---

## Self-Review 记录

1. **Spec coverage**：需求四层复核→Task 1–4；F1/T1/T2/T3→Task 5–8；新发现→Task 9；文档同步→Task 10；全量→Task 11；验收/交付→Task 12–13。
2. **Placeholder scan**：所有测试与实现代码为可执行内容，无 TBD。
3. **Type consistency**：`to_minor_units`、`_materiality_minor`、`calculate_items`、`load_rule_pack`、`extract_facts`、`CASH_GROUPS`、`AdjustmentBridgeResult`、`NormalizedInputBundle`、`AccountBalance` 均与现有代码/测试签名一致。
