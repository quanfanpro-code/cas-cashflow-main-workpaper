---
name: cas-cashflow-main-workpaper
description: 使用工作底稿法，根据单体企业审定资产负债表、利润表、科目余额表、一借一贷明细和上年度审定现金流量表，编制并复核现金流量表正表。适用于一般企业、银行、证券、保险和其他金融企业的现金流量表主表编制、账表差异核对及暂编结果复核；不用于合并报表或现金类序时账逐笔分类的直接法。
---

# 现金流量表主表工作底稿

## 适用范围

从审定报表和账务资料取得数据，在程序内部完成账表核对、调整桥接、逐项目组成计算和正表勾稽。支持一般企业、银行、证券、保险和其他金融企业的单体现金流量表。

本skill只使用工作底稿法编制现金流量表主表：

- 不处理合并现金流量表。
- 不使用现金类序时账逐笔分类的直接法。
- 不重新配对分录；输入的一借一贷明细必须已经拆分完成，Skill使用其中现成的借贷科目关系作为工作底稿补充证据。
- 不要求用户手工维护Excel计算底稿；skill自动生成可见、可复核的计算验证底稿，并同步保存结构化留痕。

## 固定输入

一次选择审定资产负债表、审定利润表、科目余额表、已拆好的一借一贷明细、上年度审定现金流量表和实际执行重要性水平。一借一贷明细最少字段为借方科目、贷方科目、配对金额；现金流标签不是必需字段，存在时只作提示和冲突证据。

账表调整明细和审计调整明细可以提供，但不是固定必需资料。缺少时先自动计算“账面数—账表调整—审计调整—审定数”差额并推导候选性质。

保险企业根据报表项目和所属期间自动选择2018旧格式或2023新保险合同准则格式；无法唯一判断时先按首选格式生成暂编表并列入待确认事项。银行、证券、保险和其他金融企业均使用财政部金融企业格式，发行债券收到的现金独立列示。

## 执行流程

```powershell
python scripts\engine_launcher.py prepare --select
python scripts\engine_launcher.py status --run-dir "运行目录"
python scripts\engine_launcher.py finalize --run-dir "运行目录" --decisions "确认结果.json"
```

prepare依次完成输入哈希冻结、标准化、账务核对、行业识别、账面报表、调整桥、事实提取、逐项目计算、完整性检查、正表及计算验证底稿输出。

## 自动判断和人工升级

- 确定性规则先行，AI必须给唯一首选及金额。
- 低于实际执行重要性水平，或没有强冲突，自动落数并留痕。
- 只有金额达到实际执行重要性水平且存在强冲突，才升级人工。
- 升级时仍输出完整暂编正表；未解决关键调整前不得标记为最终正表。
- 金额不平、漏项、重复占用和现金变动不平直接阻断，不把计算错误推给人工。

## 输出

Excel固定包含两张可见工作表：“现金流量表”和“计算验证底稿”，无外链、无隐藏Sheet。

- “现金流量表”的本期、上期金额均为冻结数值；本期金额来自本次计算，上期数只搬取、不复算。
- “计算验证底稿”按正表项目逐项列示组成、原始金额、加减方向、计入金额、来源定位、规则核验、非现金剔除、特殊事项调整、受限资金处理、自动分类依据、外部标签、标签冲突及状态。
- 每个项目均以内部复核公式列示“规则计算结果—正表列示金额—验证差异—核对结论”；这些公式只在当前工作簿内复核，不代替程序计算。
- 正表项目与对应验证区双向链接；账表调整桥及孤立调整、未映射科目、自动判断的支持和相反证据、受限资金期初期末、账务核对明细、上期项目匹配、现金变动差异及暂编/最终状态在底稿中可见。
- 逐组成机器证据继续保存在运行目录，和Excel计算验证底稿使用同一份冻结计算结果。

## 按需读取

- 输入字段：`references/协议/正式输入协议.md`
- AI和人工升级：`references/协议/AI判断与人工升级协议.md`
- 状态与最终门槛：`references/协议/正表输出与运行状态协议.md`
- 公式依据：`references/公式核验/`及`references/依据/来源索引.md`
